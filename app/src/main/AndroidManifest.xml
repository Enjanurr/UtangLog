<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Utanglog"
        tools:targetApi="31">
        <activity
            android:name=".screens.debtdetail.DebtDetailActivity"
            android:exported="false" />
        <!-- DisplayDebtActivity should be the MAIN/LAUNCHER -->
        <activity
            android:name=".screens.displayDebt.DisplayDebtActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- AddDebtActivity should NOT be launcher -->
        <activity
            android:name=".screens.addDebt.AddDebtActivity"
            android:exported="false" />
    </application>

</manifest>